
---@type FileProcessor
local local_file_processor = {
    process_song = function (song)

    end,
    song_list_from_paths = function (self, full_and_short_path_pair)
        return {}
    end,
}

return local_file_processor
